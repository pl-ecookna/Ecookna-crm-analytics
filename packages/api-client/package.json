{
  "name": "@ecookna/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@ecookna/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "echo 'api-client build is not required yet'",
    "lint": "echo 'No linter configured'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
